server:
  port: 5003
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
#    serviceUrl:
#      defaultZone: http://rio-discovery/eureka
#  instance:
#    hostname: rio-discovery
spring:
  application:
    name: comment-service